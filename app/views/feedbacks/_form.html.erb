<%= form_with(model: feedback) do |form| %>
  <div class="field">
    <%= form.text_area :body, class: 'form-control z-depth-1 mb-3', rows: 3 %>
  </div>

  <div class="actions mb-4">
    <%= form.submit %>
  </div>
<% end %>
